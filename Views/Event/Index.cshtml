@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_PublicLayout.cshtml";
}

<div ng-controller="home" class="fs-wrap">
    <div ng-repeat="c in layout.content">
        <content-layout layout="c"></content-layout>
    </div>
</div>
